import { createClient } from "@/lib/supabase/server";
import { updateGuest } from "../../actions";
type Option={value:string;label_en:string};type Answer={id:string;guest_id:string|null;answer:unknown;rsvp_questions:{label_en:string;options:Option[]}|null};type Guest={id:string;invitation_id:string;name:string;category:string;attendance:string;dietary_restrictions:string|null;notes:string|null;invitations:{code:string}|null};
const displayAnswer=(answer:unknown,options:Option[]=[])=>{if(typeof answer==='boolean')return answer?'Yes':'No';const values=Array.isArray(answer)?answer:[answer];return values.map(value=>options.find(option=>option.value===value)?.label_en??String(value??'')).join(', ')};
export default async function Guests(){const s=await createClient();const [{data:guestData},{data:answerData}]=await Promise.all([s.from('guests').select('id,invitation_id,name,category,attendance,dietary_restrictions,notes,invitations(code)').order('name'),s.from('rsvp_answers').select('id,guest_id,invitation_id,answer,rsvp_questions(label_en,options)')]);const guests=(guestData??[]) as unknown as Guest[];const answers=(answerData??[]) as unknown as (Answer&{invitation_id:string})[];return <><p className="eyebrow">Everyone</p><h1 className="font-editorial my-6 text-6xl">Guests & answers</h1><p className="mb-8 max-w-xl text-sm leading-6 opacity-70">Update each guest’s RSVP and review answers submitted through your custom form.</p><div className="grid gap-5 xl:grid-cols-2">{guests.map(g=><form action={updateGuest} className="bg-[#f8f4ed] p-6" key={g.id}><input type="hidden" name="id" value={g.id}/><p className="eyebrow mb-2">{g.invitations?.code}</p><h2 className="font-editorial text-3xl">{g.name}</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-[10px] uppercase tracking-widest">Guest type<select className="field" name="category" defaultValue={g.category}><option value="adult">Adult</option><option value="child">Child</option><option value="baby">Baby</option></select></label><label className="text-[10px] uppercase tracking-widest">Attendance<select className="field" name="attendance" defaultValue={g.attendance}><option value="pending">Pending</option><option value="accepted">Accepted</option><option value="declined">Declined</option></select></label><label className="text-[10px] uppercase tracking-widest sm:col-span-2">Dietary restrictions<input className="field" name="dietary_restrictions" defaultValue={g.dietary_restrictions??''}/></label><label className="text-[10px] uppercase tracking-widest sm:col-span-2">Private notes<textarea className="field min-h-20" name="notes" defaultValue={g.notes??''}/></label></div>{answers.filter(a=>a.guest_id===g.id||(a.guest_id===null&&a.invitation_id===g.invitation_id)).length>0&&<div className="mt-7 border-t border-black/10 pt-5"><p className="eyebrow mb-4">Submitted answers</p><dl className="space-y-4">{answers.filter(a=>a.guest_id===g.id||(a.guest_id===null&&a.invitation_id===g.invitation_id)).map(a=><div key={a.id}><dt className="text-xs font-semibold">{a.rsvp_questions?.label_en}</dt><dd className="mt-1 text-sm opacity-70">{displayAnswer(a.answer,a.rsvp_questions?.options)}</dd></div>)}</dl></div>}<button className="button mt-6">Save guest</button></form>)}</div></>}
