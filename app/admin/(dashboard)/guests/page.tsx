import { createClient } from "@/lib/supabase/server"; import { updateGuest } from "../../actions";
export default async function Guests(){const s=await createClient();const {data}=await s.from('guests').select('id,name,category,attendance,dietary_restrictions,notes').order('name');return <><p className="eyebrow">Everyone</p><h1 className="font-editorial my-6 text-6xl">Guests</h1><p className="mb-8 max-w-xl text-sm leading-6 opacity-70">Update each guest’s RSVP and private notes. Changes are saved directly to the guest list.</p><div className="grid gap-5 xl:grid-cols-2">{(data??[]).map(g=><form action={updateGuest} className="bg-[#f8f4ed] p-6" key={g.id}><input type="hidden" name="id" value={g.id}/><h2 className="font-editorial text-3xl">{g.name}</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-[10px] uppercase tracking-widest">Guest type<select className="field" name="category" defaultValue={g.category}><option value="adult">Adult</option><option value="child">Child</option><option value="baby">Baby</option></select></label><label className="text-[10px] uppercase tracking-widest">Attendance<select className="field" name="attendance" defaultValue={g.attendance}><option value="pending">Pending</option><option value="accepted">Accepted</option><option value="declined">Declined</option></select></label><label className="text-[10px] uppercase tracking-widest sm:col-span-2">Dietary restrictions<input className="field" name="dietary_restrictions" defaultValue={g.dietary_restrictions??''}/></label><label className="text-[10px] uppercase tracking-widest sm:col-span-2">Private notes<textarea className="field min-h-20" name="notes" defaultValue={g.notes??''}/></label></div><button className="button mt-6">Save guest</button></form>)}</div></>}
