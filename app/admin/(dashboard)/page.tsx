import { createClient } from "@/lib/supabase/server";
export default async function Dashboard(){const s=await createClient();const {data}=await s.from('guests').select('attendance');const guests=data??[];const stats=[['Total guests',guests.length],['Confirmed',guests.filter(g=>g.attendance==='accepted').length],['Pending',guests.filter(g=>g.attendance==='pending').length],['Declined',guests.filter(g=>g.attendance==='declined').length]];return <><p className="eyebrow">Overview</p><h1 className="font-editorial my-6 text-6xl">Dashboard</h1><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([n,v])=><div className="bg-[#f8f4ed] p-6" key={n}><p className="text-base uppercase tracking-widest">{n}</p><p className="font-editorial mt-6 text-6xl">{v}</p></div>)}</div></>}
