import { login } from "../actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;unauthorized?:string}>}){const {error,unauthorized}=await searchParams;return <main className="grid min-h-screen place-items-center px-6"><form action={login} className="w-full max-w-sm"><p className="eyebrow">Together 2027</p><h1 className="font-editorial mt-5 text-6xl">Admin</h1><label className="mt-10 block text-base uppercase tracking-wider" htmlFor="email">Email</label><input className="field" id="email" name="email" type="email" required/><label className="mt-6 block text-base uppercase tracking-wider" htmlFor="password">Password</label><input className="field" id="password" name="password" type="password" required/>{error&&<p className="mt-5 text-lg text-red-800">Invalid credentials.</p>}{unauthorized&&<p className="mt-5 text-lg text-red-800">This account is not authorized for the back office.</p>}<button className="button mt-8 w-full">Sign in</button></form></main>}
