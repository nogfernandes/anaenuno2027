"use client";
import { useState } from "react"; import { useRouter } from "next/navigation";
export function RsvpEntry({lang}:{lang:'pt'|'en'}){const [code,setCode]=useState('');const [error,setError]=useState('');const router=useRouter();return <form className="mx-auto mt-9 max-w-md" onSubmit={e=>{e.preventDefault();const clean=code.trim().toUpperCase();if(clean.length<8){setError(lang==='pt'?'Confirma o código e tenta novamente.':'Check the code and try again.');return}router.push(`/rsvp?code=${encodeURIComponent(clean)}&lang=${lang}`)}}><label className="sr-only" htmlFor="code">{lang==='pt'?'Código do convite':'Invitation code'}</label><input id="code" className="field text-center uppercase tracking-[.16em]" value={code} onChange={e=>setCode(e.target.value)} placeholder="ANA-NUNO-2027-X7K3" autoComplete="off"/><button className="button mt-7" type="submit">{lang==='pt'?'Continuar':'Continue'}</button>{error&&<p className="mt-4 text-lg" role="alert">{error}</p>}</form>}
